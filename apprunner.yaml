version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - ls -l
      - npm install --verbose
      - npm run build --verbose
run:
  pre-run:
    - npm run typeorm migration:run
  command: node dist/src/main.js
  network:
    port: 5000
